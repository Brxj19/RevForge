# RevForge Frontend

React + TypeScript + Vite foundation for the RevForge interface. This package includes the application shell, placeholder routes, shared state components, and a typed API client.
