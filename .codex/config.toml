# Project-scoped Codex defaults for RevForge.
# Custom agent files in .codex/agents/ are discovered automatically.
# This file is recommended, but the agent files themselves are sufficient.

project_root_markers = [".git", ".hg", ".sl"]

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
